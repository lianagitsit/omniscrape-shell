name: Deploy UI

on:
  push:
    branches:
      - main

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    defaults:
          run:
            working-directory: ui
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: npm ci

      - name: Build static files
        run: npm run build

      - name: Login to SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          # script: whoami
          script: |
            whoami
            ls
            scp StrictHostKeyChecking=no -r build/ ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }}:/var/www/app/ui


      # test
      # - name: Copy static files
      #   run: |
      #     mkdir -p $HOME/.ssh && touch $HOME/.ssh/known_hosts
      #     echo "${{ secrets.SSH_HOST_KEY }}" >> $HOME/.ssh/known_hosts 
      #     scp -r build/ ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }}:/var/www/app/ui

      # - name: Restart nginx
      #   uses: appleboy/ssh-action@master
      #   with:
      #     host: ${{ secrets.SSH_HOST }}
      #     username: ${{ secrets.SSH_USERNAME }}
      #     password: ${{ secrets.SSH_KEY }}
      #   script: |
      #     sudo service nginx restart
